import { FC } from "react";

const Home: FC = () => {
  return <section id="home" className="bg-[url('background.png')] home bg-no-repeat bg-cover filter brightness-90 z-[-1]">
    Witaj na stronie
  </section>;
};

export default Home;
