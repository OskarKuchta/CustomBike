import { FC } from "react";

const Personalize: FC = () => {
  return (
    <section className="personalize-page flex justify-center items-center">
    </section>
  );
};

export default Personalize;
